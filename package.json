{
  "name": "ai-personal-brain",
  "version": "1.0.0",
  "description": "AI Personal Digital Brain - Knowledge Management System",
  "license": "MIT",
  "author": "Xuan Thuc",
  "repository": "https://github.com/xuanthuc/ai-personal-brain",
  "private": true,
  "keywords": ["ai", "education", "knowledge-graph", "nlp", "react", "node.js"],
  "scripts": {
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "server:prod": "cd server && npm run start:prod",
    "client:build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup": "cd server && cp .env.example .env && npx prisma migrate dev",
    "setup:prod": "cp .env.production.example .env && cd server && npx prisma generate && npx prisma migrate deploy",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:restart": "docker-compose restart"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {}
}
